# Financial Statement PDF Extractor

Extracts the income statement, balance sheet, and cash flow statement from PDFs in `inputs/` and writes one Excel workbook and one JSON document per source PDF to `outputs/`.
